models:
  "<optimizer_model_name>":
    api_type: "openai"
    base_url: "<your base url>"
    api_key: "<your api key>"
    temperature: 0
    top_p: 1  # Optional. Defaults to 1.0 if omitted.
    # USD per 1K tokens. Required for cost tracking.
    input_price: 0.003
    output_price: 0.015
  "<executor_model_name>":
    api_type: "openai"
    base_url: "<your base url>"
    api_key: "<your api key>"
    temperature: 0
    top_p: 1  # Optional. Defaults to 1.0 if omitted.
    input_price: 0.00015
    output_price: 0.0006
